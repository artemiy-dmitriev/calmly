version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"  # Match your calmly environment if possible

python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py

# Skip building pull request docs?
# pull_request_build: false
